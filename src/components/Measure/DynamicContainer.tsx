import type { IReportDetail } from "../../types/basic";

// eslint-disable-next-line no-empty-pattern
export default function DynamicContainer({}: {data: IReportDetail}) {
  
  return (
    <div className="">
      
    </div>
  );
}